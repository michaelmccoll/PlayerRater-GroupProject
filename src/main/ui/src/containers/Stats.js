import React from 'react';
import './Stats.css';

const Stats = ({teamId,playerId}) => (
    <div>
        <h2>Player Stats</h2>
        <h4>All the stats go here...</h4>
    </div>
);

export default Stats;